name: shopping_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2  # Để gọi API (nếu cần)
  json_annotation: ^4.9.0  # Xử lý JSON

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/products.json  # Khai báo file products.json
    # Hoặc khai báo toàn bộ thư mục nếu có nhiều file:
    # - assets/